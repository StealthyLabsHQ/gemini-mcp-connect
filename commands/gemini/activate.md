---
description: Activate Gemini with your API key. Usage: /gemini:activate YOUR_API_KEY
---

Call the MCP tool `activate_gemini` with `api_key="$ARGUMENTS"`.

Display the result to the user. If no arguments are provided, ask the user to provide their Gemini API key (get one free at https://aistudio.google.com/apikey).
