---
description: Query Google Gemini AI. Usage: /gemini [--tier lite|flash|pro] <prompt>
---

Run the following bash command with the provided arguments and return the full output verbatim:

```bash
python "C:/Users/stealthy/.claude/plugins/gemini_bridge.py" $ARGUMENTS
```

If the command fails (missing API key, network error, rate limit), report the error clearly and suggest `--tier flash` or `--tier lite` as alternatives.
